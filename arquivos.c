#include <arquivos.h>
#include <stdio.h>

void ler_vetoriza_arquivo(arqv[])   
{       
    int tam =sizeof(arqv[]/sizeof(int));
    for(int i=1; i<=tam;i++)
        fopen(arqv[i],r); //abre o arquivo para ler.
        fopen(arq[tam],a);
    fscanf(arq[i],"%d",arq[tam]);
}
teste12